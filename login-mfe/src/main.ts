import { bootstrapApplication } from '@angular/platform-browser';
import { LoginElementComponent } from './app/login-element.component';

bootstrapApplication(LoginElementComponent).catch(err => console.error(err));
